# moss — PRD (Product Requirements Document)

> 생각은, 머무는 동안 형태가 된다.
> Flow, not Folders.

**상태**: 초안 v0.1
**결정일**: 2026-05-19
**동반 문서**: `docs/moss.blueprint.json` (요구사항·기능·플로우·화면 구조의 시각화)

본 문서는 `SPEC.md`, `TECH_SPEC.md`, `UI_SPEC.md` 세 파일을 단일 PRD로 통합한 것이다. 백로그·요구사항·기능 트리·플로우는 블루프린트 패널에서, 서술·와이어프레임·기술 결정은 이 문서에서 본다.

---

# Part I — 제품

## 1. 제품 요약 (Product Summary)

- **제품명**: moss
- **카테고리**: 사고 작업대 / 생각 OS / 창의적 사고 환경
- **한 줄 정의**: 생각이 사라지지 않게 만드는 느슨한 사고 작업대.
- **태그라인 (히어로)**: "흩어진 생각을 한 곳에 머물게 하고, 서로 연결되며, 당신의 방향이 보이게 합니다."

기존 생산성 툴이 "정리·관리·완료"를 중심으로 설계되었다면, moss는 **"머무름·응집·연결"** 을 중심으로 설계된다.

핵심 문제는 기록 부족이 아니다. 생각이 **군집이 생기기도 전에 다음 자극에 밀려 사라진다**는 점이다.

moss는 저장된 정보를 관리하는 툴이 아니라:
1. 생각이 오래 살아있게 하고
2. 서로 느슨하게 연결되게 하며
3. 미완성 상태를 유지한 채
4. 자연스럽게 초안과 작업으로 이어지게 하는

**생각 체류 환경**을 목표로 한다.

---

## 2. 비전과 철학

### 2-1. POSIWID — 시스템의 목적은 그것이 실제로 하는 일이다

브랜드는 말이 아니라 실제로 하는 일이다. 모든 인터랙션은 아래 6가지 질문으로 검증한다.

- 생각을 이어지게 만드는가? / 끊는가?
- 미완성을 허용하는가? / 정리 강박을 유도하는가?
- 다시 돌아오게 만드는가? / 사용자를 압박하는가?

### 2-2. 시장 포지셔닝

| 기존 툴 | moss |
|---|---|
| 정리 | 머무름 |
| 완료 | 숙성 |
| 생산성 | 사고 흐름 |
| 구조 | 장(field) |
| 데이터베이스 | 작업대 |
| 검색 | 재노출 |
| 카테고리 | 분위기 |

### 2-3. 핵심 동사

| 제품 | 핵심 동사 |
|---|---|
| Notion | 정리 |
| Pinterest | 저장 |
| FigJam | 배치 |
| **moss** | **머무름** |

---

## 3. 타깃 사용자

### 3-1. 1차 사용자

**실용적 창작자** — 창작과 업무의 경계가 흐려진 사람들.

- PM / 마케터 / 브랜드 운영자
- 1인 사업자 / 기획자
- 디자이너 / 콘텐츠 제작자
- AI 활용 지식 노동자

### 3-2. 사용자 특성

- 여러 도메인을 동시에 탐색
- 저장량 많음, 연결형 사고, 영감 기반 작업
- 미완성 상태 많음, 정보 과잉 피로 존재

### 3-3. 현재 대체 행동 (이걸 대신한다)

카톡 나에게 보내기 / 스크린샷 폴더 / 인스타 저장 / 텔레그램 메모 / 노션 임시 페이지 / 브라우저 탭 과다 / 유튜브 좋아요 / 피그잼 보드 — 모든 생각이 흩어져 있음.

---

## 4. 목표 / 제외 사항 / 완료 기준

### 4-1. 목표

- 캡처 → 노출 → 연결 → 응집 → 초안 전환의 흐름을 끊김 없이 제공한다.
- 사용자가 **정리하지 않아도 생각이 살아있는 구조**를 만든다.
- AI는 생성기가 아니라 **응집기·거울**로 동작한다.
- 자유 캔버스(giant canvas) 기반의 사고 장(field)을 제공한다.

### 4-2. 제외 사항 (MVP에서 명시적으로 제외)

- 팀 협업 / 여러 사용자 동시 편집
- 업무·태스크 관리 (할일, 마감, 담당자)
- 캘린더 / 프로젝트 관리
- AI 글쓰기·콘텐츠 자동 생성 (대필)
- 정량 생산성 지표 (연속 기록일·생산성 점수·완료율 같은 것)

### 4-3. 완료 기준

다음 5개 핵심 지표가 측정 가능한 상태로 출시된다.

| 지표 | 정의 |
|---|---|
| 생각 회귀율 (Thought Return Rate) | 저장한 메모를 7일 내 다시 여는 비율 |
| 연결 생성률 (Connection Rate) | 메모 간 연결(수동 + AI 채택) 생성 비율 |
| 재방문 체류 시간 (Revisit Time) | 같은 메모에 다시 머무는 평균 시간 |
| 초안 전환율 (Draft Conversion) | 메모 → 초안(문서·보드) 전환 비율 |
| 장기 유지율 (Long-term Retention) | 30일 / 90일 재방문률 |

### 4-4. 플랫폼과 기술 방향 (2026-05-19 확정)

| 영역 | 결정 | 함의 |
|---|---|---|
| 플랫폼 | **웹 단독 (PWA)** | 설치 장벽 없음. 링크 공유로 진입. 모바일은 반응형으로 동일 코드베이스. 캔버스 성능은 WebGL/Canvas2D 보강 필요. |
| 데이터 | **로컬 우선 (Local-first)** | 브라우저 저장소(IndexedDB + OPFS)가 원본. 여러 기기 동기화는 Pro 기능으로 분리. 오프라인 작동 기본. |
| AI | **API 우선 (Claude / OpenAI)** | 백엔드 프록시로 키 보호 + 프리미엄 쿼터 관리. 임베딩·요약·연결 제안 모두 서버 경유. |
| 수익 | **프리미엄 구독** | Free = 기본 캔버스 + 제한된 AI 호출 / Pro = AI 풀 + 여러 기기 동기화 + 백업. 가격·쿼터는 별도 결정. |

**함의에서 파생되는 제약**

- **로컬 우선 × API AI**: 로컬 메모를 AI에 보낼 때 어떤 데이터가 서버로 가는지 사용자에게 투명해야 한다. "AI에 보낸 메모" 시각적 표시 + 옵트인 토글 필요.
- **웹 × 거대 캔버스**: 메모 수가 늘면 DOM 기반 렌더링은 한계. 가상화 + Canvas2D/WebGL 렌더러 검토.
- **PWA × 오프라인**: 서비스 워커로 앱 셸·에셋 캐싱, 데이터는 IndexedDB. AI 기능만 온라인에 의존.

---

## 5. 핵심 경험 원칙

1. **정리를 늦춘다.** 저장 즉시 제목/폴더/태그/카테고리를 요구하지 않는다. 기본 상태는 "일단 남긴다."
2. **미완성을 허용한다.** 제목 없는 메모, 스크린샷 하나, 링크 하나 모두 충분한 사고 재료다.
3. **재노출 기반 시스템.** 홈 피드는 최신순이 아니라 "최근 머문 생각 / 반복 키워드 / 연결 가능한 메모 / 오래 묵혔지만 살아있는 생각" 순서다.
4. **AI는 응집기다.** 글 대필이 아니라 사고 흐름 분석, 반복 패턴 발견, 연결 제안, 감정/질감 요약.

---

## 6. 정보 구조 (Information Architecture)

```
moss/
├── 작업공간(Workspace) — 메인 화면 (도구 사이드바 + 캔버스)
│   ├── 시스템 보드 "머무는 생각"   (첫 진입, AI 자동 큐레이팅)
│   ├── 사용자 보드들              (자유 배치)
│   └── 보드 선택 드롭다운          (상단 헤더)
└── 시그널스(Signals)  — AI 패널 (우측에서 미끄러져 나옴)
```

**별도 화면 없음**:
- 홈 재노출 피드 → 시스템 보드 "머무는 생각"에 카드로 흡수
- 보드 목록 화면 → 상단 헤더의 보드 드롭다운으로 흡수
- 캡처 화면 → 작업공간 좌측 도구 사이드바 (끌어다 놓기). 단축키·모바일 캡처 바는 보조 경로

**부수 화면**: 온보딩(최초 1회), 내보내기(Cmd+E 모달), 페이월(자동 트리거 모달), 모바일 피드(모바일 전용).

---

## 7. 기능 명세

### 7-1. 시스템 보드 "머무는 생각" — 첫 진입 기본 보드

별도 홈 화면 없음. 앱 시작 시 SCR-workspace가 열리며 기본 보드는 자동 큐레이팅 시스템 보드 "머무는 생각". 삭제 불가. 보드 선택 드롭다운의 첫 항목으로 고정.

#### 캔버스 내 카드 구성

| 카드 종류 | 내용 | 생성 방식 |
|---|---|---|
| 지금 머무는 생각 (×n) | 최근 자주 회귀한 주제 | AI 자동 (행동 기반) |
| 다시 떠오른 생각 | N일 전 메모와 최근 메모 연결 resurfacing | AI 추천 |
| 오늘의 연결 | 멀리 있는 두 메모의 연결 제안 (수락/거절) | AI 추천 |
| 사고 흐름 타임라인 | 최근 사고 이동 흐름 | 자동 |
| 오늘의 질문 (위젯 카드) | 사용자가 메타 질문을 직접 입력 | 사용자 입력 |
| 생각 일기 (위젯 카드) | 캘린더 + 날짜별 메모 dot | 자동 |
| 연결 히스토리 (위젯 카드) | 연결 생성 순서 타임라인 | 자동 |

#### 시스템 보드 동작 원칙

- **사용자 메모를 직접 저장하지 않음** — 캐치만 큐레이팅. 도구를 시스템 보드에 드래그하면 "새 보드를 만들까요?" 토스트.
- 위젯 카드(오늘의 질문 / 생각 일기 / 연결 히스토리)는 일반 카드와 시각 구분 + 삭제 불가
- 빈 상태(메모 < 10): 큐레이팅 카드 대신 "조금 더 머무르면 패턴이 보일 거예요" 안내
- 모바일에서는 SCR-mobile-feed · Mobile Feed로 같은 카드를 리스트 뷰로 표시

---

### 7-2. 작업공간(Workspace) — 도구 사이드바 + 캔버스 (메인 화면)

핵심 작업 공간. 좌측 도구 사이드바의 캡처 도구를 캔버스로 끌어다 놓는다. "살아있는 사고의 장(field)" 느낌.

#### 레이아웃

- **좌측 사이드바 (접이식)**: 펼침 220px / 접힘 64px. 도구 10종(끌기 시작점) + 구분선 + 시그널스 · 내보내기 메뉴. `[` 키로 펼침/접힘 토글.
- **상단 헤더**: 보드 선택 드롭다운 ("머무는 생각 ▾") + 검색 + 알림 + 프로필.
- **우측 캔버스**: 무한 캔버스 (놓기 영역). DOM + 가상 스크롤(MVP) → Canvas2D 폴백(메모 200개 초과 시).

#### 핵심 사용성

- **끌어다 놓기**: 도구를 캔버스에 놓으면 → 빈 카드 생성 + 타입별 즉시 편집 시작
- **자유 배치**: 카드 좌표 기반, ±1.5° 미세 회전 (종이 질감)
- **연결선**: 수동 연결(실선) + AI 추천 연결(점선). 카드 위로 마우스를 올리면 연결 핸들이 나타남.
- **줌 기반 UX**: 확대 → 세부 메모, 축소 → 사고 지형도. 우하단 `[−] 100% [+] [▦]`
- **분위기 기반 보드**: 폴더 대신 추상 컬렉션. 보드 이름은 비워도 됨.

#### 보드 선택 드롭다운

상단 헤더의 보드 이름 클릭(또는 `Cmd+P`)으로 열림. 항목:

1. **머무는 생각** (시스템, 첫 항목 고정, 체크 표시)
2. 사용자 보드들 (최근 수정 순)
3. `+ 새 보드` → 5종 시작 템플릿 선택

`Cmd+B`로 시스템 보드 ↔ 마지막 일반 보드 토글.

#### 캔버스 요소

캡처 10종(§7-3) + 연결선 + 손글씨 라벨 + 시스템 위젯 카드 + 장식 요소(테이프, 핀, 모서리 접힘 효과).

#### 인터랙션 디테일

- 카드 드래그 : 위치 이동, 미세 회전 유지
- `Cmd + 드래그` : 여러 카드 동시 선택
- 두 카드 사이에 마우스 hover : 연결 핸들 → 다른 카드로 끌어 연결선 생성
- 더블 클릭 : 카드 자리에서 편집
- 우클릭 : 색상 / 연결 / 보드 이동 / 삭제 / AI 처리 거부(aiOptOut) 토글

---

### 7-3. 캡처(Capture) — 도구 끌어다 놓기 + 단축키 보조 경로

> 목표: 입력 시간 2초 이내. 입력 직후 제목·폴더·태그 요구 없음.
> "지금은 그냥 남겨두셔도 좋아요."

#### 데스크톱 메인 경로: 도구 사이드바 끌어다 놓기

좌측 사이드바의 10종 도구 중 하나를 캔버스로 끌어다 놓으면 → 그 좌표에 빈 카드 + 즉시 편집 시작.

| 도구 | 놓기 직후 |
|---|---|
| Aa 텍스트 | 빈 카드 + 텍스트 커서 활성 |
| ✎ 손글씨 | 그리기 모드 (Apple Pencil/트랙패드) |
| ⌒ 마인드맵 | 루트 노드 하나만 있는 카드 |
| ⌘ 하이라이트 | 인용 입력칸 + 출처 메타 |
| ☑ 체크리스트 | 빈 항목 1개짜리 카드 (완료 압박 없음) |
| 🖼 이미지 | 파일 선택 창 또는 클립보드 붙여넣기 (OCR 인덱싱) |
| 🔗 링크 | URL 입력 → 제목/요약/썸네일 자동 미리보기 |
| 🎤 음성 | 즉시 녹음 시작 (Whisper API로 자동 받아쓰기 옵션) |
| 📎 파일 | PDF/문서/영상 선택, 미리보기 가능 포맷 우선 |
| </> 코드 | 빈 카드 + 언어 자동 감지 + 구문 강조 |

#### 보조 진입점

- 전역 단축키 `Cmd+Shift+N` (마지막 사용 도구로 즉시 빈 카드, 캔버스 중앙)
- 도구별 단축키 `Cmd+1` ~ `Cmd+0`
- 모바일 하단 캡처 바 (드래그 대신 탭 → 하단 시트가 올라옴)
- 브라우저 확장 (링크 / 하이라이트 / 스크린샷)

---

### 7-4. 시그널스(Signals · AI 레이어) — "사고 거울"

조용히 비춰주는 AI. 비서(Assistant)가 아니다.

| 기능 | 출력 형태 |
|---|---|
| 반복 키워드 탐지 | 키워드 + 빈도 목록 (예: `신뢰 23 / 경험 17 / 시간 14 / 안심 12 / 흐름 11`) |
| 사고 군집 시각화 | 최근 메모 기반 군집 그래프 (노드/엣지) |
| 흐름 요약 | 자연어 한두 문장 ("최근 7일 동안 142개의 생각을 남기셨어요. 그 중 18%가 서로 연결되었습니다.") |
| 느슨한 연결 제안 | 거리가 먼 두 메모를 짝지어 제시 |
| 사고 리듬 분석 | 시간대별 사고 유형 (오전 구조화 / 밤 감정 / 영상 레퍼런스 집중 시기) |

#### 시그널스 UX 원칙

- 자동 생성된 인사이트는 **닫기 가능**해야 한다.
- AI의 답을 강제하지 않는다. **"이 연결이 흥미로우신가요?"** 같은 톤.
- 사용자가 거절한 제안은 학습 신호로 반영된다.

---

### 7-5. 보드 선택 — 분위기 기반 느슨한 컬렉션 (별도 화면 아님)

별도 보드 목록 화면 없음. SCR-workspace 상단 헤더의 드롭다운으로 관리한다. "생각이 오래 머무는 장."

- 보드 이름은 추상적/감각적 (`soft reassurance`, `things that linger`, `systems that breathe`, `unfinished`, `why people stay` 같은 식)
- 빈 이름 허용
- 분위기 기반 — 정보보다 질감 중심
- AI가 새 보드와 어울리는 메모를 자동 제안 (수락하면 카드 추가)

#### 5종 시작 템플릿

새 보드 만들 때 선택 (`Cmd+N`):

| 템플릿 | 설명 |
|---|---|
| 자유 캔버스 | 모든 것을 자유롭게 배치하고 연결 |
| 마인드 확장 | 하나의 주제를 깊고 넓게 펼침 |
| 프로젝트 보드 | 아이디어를 실행과 잇는 공간 |
| 리서치 보드 | 자료를 수집하고 인사이트를 도출 |
| 일기 보드 | 하루의 생각과 감정을 기록하는 공간 |

> "정답을 제공하지 않습니다. 당신만의 흐름을 만듭니다."

부가 위젯 카드(오늘의 질문 / 생각 일기 / 연결 히스토리)는 §7-1 시스템 보드 안의 카드로 흡수됨.

---

## 8. 비주얼 디자인 방향

### 8-1. 톤(Tone)

조용한 지능감 / 낮은 압력 / 살아있는 벽 / 연구실 작업대 / 감독의 핀보드 / 미완성 허용.

### 8-2. 컬러 시스템

| 군 | 컬러 |
|---|---|
| 베이스 | 따뜻한 화이트, 부드러운 베이지, 종이 그레이 |
| 다크 캔버스 | 진한 차콜 (#1c1c1e 계열) 위 종이/메모 카드 |
| 강조색 | 차분한 라임, 더스티 블루, 바랜 퍼플, 부드러운 옐로 |
| 금지 | 과도한 SaaS 블루, 채도 높은 단색 |

### 8-3. 타이포그래피

- 에디토리얼 톤 / 넉넉한 여백 / 저밀도 UI
- 본문 한글: 자족적 세리프 또는 휴머니스트 산세리프
- 카피: 큰 글자 + 넉넉한 자간·행간

### 8-4. 모션

- 빠른 마이크로 인터랙션보다 **부드러운 등장, 느린 연결, 잔향 있는 애니메이션**
- "떠오르는 느낌"
- 카드 등장: 80~120ms 이즈아웃, 미세 회전 (±1.5°)
- 연결선 그려지는 애니메이션: 400ms

### 8-5. 텍스처

- 종이 질감, 핀, 마스킹 테이프, 노트 모서리 접힘 등 물리적 디테일
- 손글씨 라벨/화살표(스케치 톤) — 디지털의 깔끔함과 대비

---

## 9. UX 안티-패턴 (절대 금지)

- 즉시 폴더/제목 강요
- "아직 아무것도 없네요!" 류의 빈 상태 압박
- streak / productivity score / completion rate
- "오늘 기록 안 하셨네요!" 류의 알림
- AI가 모든 걸 생성하거나 정답 제시

---

# Part II — 와이어프레임

> 화면 7개(SCR-*) × 케이스 23개의 레이아웃·구성 요소·상태·인터랙션을 빌더가 참고 가능한 수준으로 정의한다.
> 다루는 것: 와이어프레임 + 케이스 매핑 + 구성 요소 분해 + 상태 / 인터랙션 노트.
> 화면 ID는 `docs/moss.blueprint.json`의 `screens[].id`와 1:1 일치. 케이스별 상세 스케치는 블루프린트를 참조한다.
> 다루지 않는 것: 픽셀 단위 디자인, 토큰 정의 (별도 `DESIGN_TOKENS.md` 예정).

## 10. 레이아웃 토큰 (참고용)

| 영역 | 값 |
|---|---|
| Desktop 폭 | 1280 ~ 1920 (반응형) |
| Mobile 폭 | 320 ~ 430 |
| Tool sidebar | 펼침 220px / 접힘 64px (collapsible) |
| Top bar | 56px |
| Card 표준 폭 | 220 ~ 280 |
| Card 회전 | ±1.5° (랜덤, 종이 질감) |
| Z-index 순서 | canvas < cards < connection-lines < selection-overlay < panel < modal < toast |

---

## 11. SCR-workspace — 도구 사이드바 + 캔버스 (메인 화면)

**목적**: moss의 메인 화면. 좌측 도구 사이드바의 캡처 도구를 **끌어다 캔버스에 놓아** 메모를 만든다. 첫 진입은 시스템 보드 "머무는 생각"이고, 보드 드롭다운으로 다른 보드로 전환한다.
**진입**: 앱 시작 기본(시스템 보드) / 보드 드롭다운 / 단축키 `[`로 사이드바 펼침·접힘 토글.
**플랫폼**: 데스크톱. 모바일은 SCR-mobile-feed (드래그 대신 탭 방식).

### 케이스 (6가지)

블루프린트 `SCR-workspace.variants[]`와 1:1 매핑. 상세 와이어프레임은 `docs/moss.blueprint.json`에 있고, 본 문서는 레이아웃·구성 요소·상태·인터랙션을 다룬다.

| 케이스 ID | 설명 | 그리드 (열 / 행) | 도구 동작 |
|---|---|---|---|
| `system` | 시스템 보드 "머무는 생각" — 데이터 충분 (자동 큐레이팅 카드 7-9개) | `220px 1fr` / `56px 1fr 48px` | 드롭 시 "새 보드를 만들까요?" 토스트 (시스템 보드는 사용자 메모 직접 저장 안 함) |
| `system-empty` | 시스템 보드 — 메모 < 10 (데이터 부족) | `220px 1fr` / `56px 1fr 48px` | 큐레이팅 카드 대신 잔잔한 안내 |
| `board` | 일반 사용자 보드 — 자유 배치 (메모 + 연결선 + 손글씨 라벨) | `220px 1fr` / `56px 1fr 48px` | 드롭 = 해당 보드에 새 카드 추가 |
| `board-empty` | 빈 새 보드 — Aa 도구에 미세 펄스로 시작점 힌트 | `220px 1fr` / `56px 1fr 48px` | 첫 드롭 시 안내 카피 |
| `collapsed` | 사이드바 접힘 (아이콘만, 캔버스 폭 확장) | `64px 1fr` / `56px 1fr 48px` | 동일 — hover 시 도구명 툴팁 |
| `dragging` | 도구 → 캔버스 드래그 중 (잔상 미리보기 + 드롭 가능 영역 하이라이트) | `220px 1fr` / `56px 1fr 48px` | 드롭 시 빈 카드 + 타입별 즉시 편집 모드 |

대표 와이어프레임 3가지(`system` / `board` / `collapsed`)는 아래에 첨부. `system-empty`, `board-empty`, `dragging`은 상태·인터랙션 절에 글로 기술.

### 레이아웃 — 펼친 상태 (사이드바 220px, 시스템 보드 모드)

```
┌──────────────┬──────────────────────────────────────────────────────┐
│ ◀ TOOLS      │ moss.  ·  머무는 생각 ▾  (시스템 보드)              │
│              │ ────────────────────────────────────────────────────│
│ Aa  텍스트   │                                                      │
│ ✎   손글씨   │  ╭─ 지금 머무는 ─╮  ╭─ 다시 떠오름 ─╮  ╭─ 오늘의 ─╮│
│ ⌒   마인드맵 │  │ 부담 없는 UX  │  │ 5일 전 메모  │  │ 연결    ││
│ ⌘   하이라이트│  │ 안심의 감각  │  │     ↕         │  │ 쿠폰 심리││
│ ☑   체크리스트│  │ 브랜드의 잔향 │  │ 어제 스크린샷 │  │   ↕     ││
│ 🖼  이미지   │  ╰──────────────╯  ╰──────────────╯  │ 관계의   ││
│ 🔗  링크     │                                       │  안심감  ││
│ 🎤  음성     │  ╭─ 사고 흐름 ─────────────────╮     │[수락]    ││
│ 📎  파일     │  │ UX 리서치 → 신뢰의 순간 →   │     │[숨기기]   ││
│ </> 코드     │  │ First Purchase → 브랜드 경험 │     ╰──────────╯│
│              │  ╰─────────────────────────────╯                    │
│ ─────────    │                                                      │
│ ✦   Signals  │  ╭─ 오늘의 질문 ─╮  ╭─ 생각 일기 ─╮  ╭─ 연결 ──╮  │
│ ↗   Export   │  │ 떠오르는 질문을│  │ 캘린더 ●  │  │ 히스토리 │  │
│              │  │ 적어보세요... │  │ 5/23 ●    │  │ 5/18 →   │  │
│              │  ╰───────────────╯  ╰───────────╯  │ 5/24     │  │
│              │                                     ╰──────────╯  │
│              │                ✎ 머무는 생각 (시스템 보드)         │
│              │                                  [−] 100% [+] [▦] │
└──────────────┴──────────────────────────────────────────────────────┘
```

### 레이아웃 — 펼친 상태 (일반 사용자 보드 예시)

```
┌──────────────┬──────────────────────────────────────────────────────┐
│ ◀ TOOLS      │ moss.  ·  Brand Exploration ▾                       │
│              │ ────────────────────────────────────────────────────│
│ Aa  텍스트   │                                                      │
│ ✎   손글씨   │      ╭───────╮            ╭──────────────╮           │
│ ⌒   마인드맵 │      │ memo  │  ╲╲       │ "신뢰는 정보가│  ╭─────╮│
│ ⌘   하이라이트│      │ 너무  │   ╲╲      │ 아니라, 안심의│  │ vid ││
│ ☑   체크리스트│      │ 많은… │    ╲     │ 순간에 생긴다"│  │  ▷  ││
│ 🖼  이미지   │      ╰───────╯     ╲     ╰──────────────╯  ╰─────╯│
│ 🔗  링크     │         │           ╲                                │
│ 🎤  음성     │         ▼            ╲          ╭── First Purchase─╮│
│ 📎  파일     │      ╭───────╮        ╲─ ─ ─ ─ ─│ 사람들은 첫 구매…││
│ </> 코드     │      │ 부담  │                  ╰───────────────────╯│
│              │      │ 없는  │                                       │
│ ─────────    │      │  UX   │       ✎ Brand Exploration             │
│ ✦   Signals  │      ╰───────╯       (handwritten label)              │
│ ↗   Export   │                                                       │
│              │                                  [−] 100% [+] [▦]   │
└──────────────┴──────────────────────────────────────────────────────┘
```

`╲╲` = washi tape, 실선 = 수동 연결, `─ ─ ─` = AI 추천 연결.

### 레이아웃 — 접힌 상태 (사이드바 64px)

```
┌────┬────────────────────────────────────────────────────────────────┐
│ ▶  │ moss.  ·  머무는 생각 ▾                                       │
│ Aa │ ──────────────────────────────────────────────────────────────│
│ ✎  │                                                                │
│ ⌒  │              (캔버스 영역 — 도구를 끌어다 놓기)                │
│ ⌘  │                                                                │
│ ☑  │                                                                │
│ 🖼 │                                                                │
│ 🔗 │                                                                │
│ 🎤 │                                                                │
│ 📎 │                                                                │
│ </>│                                                                │
│ ─  │                                                                │
│ ✦  │                                                                │
│ ↗  │                                       [−] 100% [+] [▦]         │
└────┴────────────────────────────────────────────────────────────────┘
```

### 구성 요소

| ID | 이름 | 비고 |
|---|---|---|
| WS-sidebar | 좌측 사이드바 (접이식) | 펼침 220px / 접힘 64px. `[` 키 또는 ◀/▶ 핸들로 토글 |
| WS-tools | 캡처 10종 도구 (끌기 시작점) | Aa/✎/⌒/⌘/☑/🖼/🔗/🎤/📎/</>. 끌어다 놓기 가능 + 단일 클릭 → 캔버스 중앙 배치 |
| WS-divider | 구분선 | 도구 영역과 메뉴 영역 분리 |
| WS-nav | 시그널스 / 내보내기 메뉴 | 클릭 시 패널/모달 — 도구 아님 |
| WS-topbar | **보드 선택 드롭다운** + 검색·알림·프로필 | 보드 이름 클릭 → 보드 목록 + "+ 새 보드" + 시스템 보드 |
| WS-board-picker | 보드 드롭다운 내용 | 첫 항목은 "머무는 생각 (시스템)" 고정. 아래에 사용자 보드. 맨 아래 "+ 새 보드" + 시작 템플릿 5종 |
| WS-canvas | 무한 캔버스 (놓기 영역) | DOM + 가상 스크롤(MVP) → 메모 200개 초과 시 Canvas2D로 전환 |
| WS-card | 메모 카드 | 일반 카드 / 시스템 위젯 카드(오늘의 질문·생각 일기·연결 히스토리) 구분 |
| WS-connection | 연결선 | 실선(수동) / 점선(AI), 라벨 옵션 |
| WS-label | 손글씨 라벨 | 보드 내 영역 이름 |
| WS-zoom | 우하단 줌 컨트롤 | `[−]` `100%` `[+]` `[▦]` (지형도 토글) |

### 상태

- **시스템 보드 — 데이터 부족 (메모 < 10)**: AI 큐레이팅 카드 대신 안내 카드 "조금 더 머무르면 패턴이 보일 거예요. 도구를 끌어다 놓아보세요."
- **시스템 보드 — 정상**: 자동 큐레이팅 카드 7-9개 배치. 도구를 놓으면 "새 보드를 만들까요?" 안내 토스트
- **일반 보드 — 비어있음**: 캔버스 중앙에 "도구를 끌어다 놓아보세요" + 첫 도구(Aa)에 미세한 펄스
- **드래그 시작**: 도구 아이콘이 커서를 따라옴 + 캔버스에 잔상 미리보기 + 놓기 가능 영역 살짝 하이라이트
- **놓기 직후**: 해당 좌표에 빈 카드 생성 + 타입별 즉시 편집 모드
- **사이드바 접힘**: 아이콘만, hover 시 도구명 툴팁
- **메모 200개 초과**: 토스트 "더 부드러운 렌더러로 전환합니다" → Canvas2D
- **AI 처리 거부 보드**: 도구는 그대로. AI 연결선/시스템 보드 큐레이팅 카드만 미표시

### 인터랙션

#### 끌어다 놓기 (핵심)

| 도구 | 놓기 직후 동작 |
|---|---|
| Aa 텍스트 | 빈 카드 + 텍스트 커서 활성 |
| ✎ 손글씨 | 빈 카드 + 그리기 모드 (Apple Pencil/트랙패드) |
| ⌒ 마인드맵 | 루트 노드만 있는 카드 (자식 노드 추가 가능) |
| ⌘ 하이라이트 | 인용 입력칸 + 출처 메타(옵션) |
| ☑ 체크리스트 | 빈 항목 1개짜리 카드 |
| 🖼 이미지 | 파일 선택 창 (또는 클립보드에서 자동 붙여넣기) |
| 🔗 링크 | URL 입력창 → 제목·요약·썸네일 미리보기 |
| 🎤 음성 | 즉시 녹음 시작 → 정지하면 카드 생성 |
| 📎 파일 | 파일 선택 창 |
| </> 코드 | 빈 카드 + 언어 자동 감지 모드 |

#### 키보드

- `[` : 사이드바 펼침/접힘 토글
- `Cmd+1` ~ `Cmd+0` : 도구 단축키 (1=Aa, 2=✎, …, 0=</>) → 마우스 위치 또는 캔버스 중앙에 즉시 카드
- `Cmd+P` : 보드 선택 드롭다운 열기 (포커스 이동)
- `Cmd+B` : 시스템 보드 ↔ 마지막 일반 보드 토글
- `Cmd+N` : 새 보드 만들기 (템플릿 선택)
- `g s` : 시그널스 패널 열기
- `Cmd+E` : 내보내기 모달 열기
- `Space + 드래그` : 캔버스 이동(팬)
- `Cmd ±` 또는 트랙패드 핀치 : 줌
- `[` 더블 탭 : 사이드바 완전 숨김 (캔버스 풀화면)

#### 캔버스 인터랙션

- 카드 드래그 : 위치 이동, 미세 회전 유지
- `Cmd + 드래그` : 여러 카드 동시 선택
- 두 카드 사이에 마우스 hover : 연결 핸들 노출 → 다른 카드로 끌어 연결선 생성
- 카드 더블 클릭 : 자리에서 편집
- 우클릭 : 상황 메뉴 (색상 / 연결 / 보드 이동 / 삭제 / AI 처리 거부(aiOptOut) 토글)
- 시스템 위젯 카드 (오늘의 질문 / 생각 일기 / 연결 히스토리) : 일반 카드와 다른 시각 구분 + 삭제 불가

---

## 12. SCR-capture-bar — 10종 빠른 입력 (보조 경로)

**목적**: 입력 시간 2초 이내. 제목·폴더·태그 강제 X. **보조 진입점** — 데스크톱 메인은 SCR-workspace의 도구 사이드바 끌어다 놓기.
**진입**: 전역 단축키 `Cmd+Shift+N` (마지막 사용 도구로 즉시 빈 카드) / 모바일 하단 캡처 바.
**플랫폼**: 데스크톱 모달(단축키 보조 경로) + 모바일 하단 시트(모바일 메인 캡처 경로)
**케이스** (블루프린트 대응): `desktop-modal` (중앙 모달 보조 경로) / `mobile-sheet` (하단에서 60% 높이로 올라옴, 모바일 메인) / `audio-recording` (🎤 도구 선택 후 파형 + 타이머)

### 레이아웃 (데스크톱 모달, 중앙)

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│   [Aa] [✎] [⌒] [⌘] [☑] [🖼] [🔗] [🎤] [📎] [</>]    × close    │
│   txt  hand mind hi   chk  img  url  aud  fil  code              │
│   ─────────────────────────────────────────────────────────      │
│                                                                  │
│   ┌──────────────────────────────────────────────────────────┐   │
│   │ 지금은 그냥 남겨두셔도 좋아요...                          │   │
│   │                                                          │   │
│   │                                                          │   │
│   │                                                          │   │
│   └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│   B  I  U  •  현재 보드: Brand Exploration ▾    [⏎] 저장        │
└──────────────────────────────────────────────────────────────────┘
```

타입을 바꾸면 입력 영역만 교체된다 (텍스트→키보드, 손글씨→캔버스, 음성→파형, 링크→URL 입력칸, 파일→파일 드롭 영역).

### 구성 요소

| ID | 이름 | 비고 |
|---|---|---|
| CAP-types | 10종 타입 토글 | 키보드 1-9, 0 |
| CAP-input | 타입별 입력 영역 | 안내 문구는 "지금은 그냥 남겨두셔도 좋아요..." |
| CAP-toolbar | 미니 툴바 | 텍스트 타입에서만 B/I/U |
| CAP-target | 저장 위치 드롭다운 | "현재 보드 / 무소속 / 새 보드". 기본값 = 현재 보드. 그대로 둬도 됨 |
| CAP-save | 저장 버튼 | `⏎` 또는 클릭 |

### 상태

- **기본**: 텍스트 타입, 빈 입력
- **이미지 타입 + 클립보드 감지**: 클립보드의 이미지를 자동 붙여넣기
- **링크 타입**: URL 입력 → 0.5초 지연(디바운스) → 제목·요약·썸네일 미리보기
- **음성 녹음 중**: 파형 + 타이머, [중지] 버튼
- **저장 직후**: 모달이 닫히며 캔버스에 카드 등장 (잔향 모션)

### 인터랙션

- ESC 또는 ×: 취소 (입력 내용 사라짐)
- ⏎ (텍스트 모드): 한 줄 → 저장 / Shift+⏎ → 줄바꿈
- Cmd+⏎: 어디서든 저장
- 타입을 바꾸면 입력 내용 보존을 시도, 호환되지 않으면 확인 모달

### 모바일 하단 시트 차이

- 화면 하단에서 60% 높이로 올라옴
- 타입 토글이 가로 스크롤
- 카메라 / 마이크 / 사진 라이브러리 빠른 진입

---

## 13. SCR-signals — AI 인사이트 패널

**목적**: 사고 거울. 키워드 / 군집 / 흐름 요약 / 리듬.
**진입**: 좌측 사이드바 ✦ 시그널스 / `g s` / 시스템 보드 "오늘의 연결" 카드에서 "더 보기"
**플랫폼**: 데스크톱 — 우측에서 미끄러져 나오는 패널(420px) / 모바일 — 전체 화면
**케이스** (블루프린트 대응): `default` (4개 섹션 — 키워드/군집/흐름 요약/리듬) / `data-empty` (메모 < 10, 잔잔한 안내) / `quota-exhausted` (마지막 캐시 + Pro 전환 안내, 잔잔한 톤)

### 레이아웃

```
┌──────────────────────────────────────────────────────┐ ┌──────────┐
│  ◯ Workspace (뒤에 흐려짐)                          │ │ Signals  │
│                                                      │ │  ×       │
│                                                      │ │──────────│
│                                                      │ │          │
│                                                      │ │ 반복 키워드│
│                                                      │ │ ─────────│
│                                                      │ │ 신뢰   23│
│                                                      │ │ 경험   17│
│                                                      │ │ 시간   14│
│                                                      │ │ 안심   12│
│                                                      │ │ 흐름   11│
│                                                      │ │          │
│                                                      │ │ 최근 연결│
│                                                      │ │ ─────────│
│                                                      │ │   ●─●    │
│                                                      │ │  ╱  ╲    │
│                                                      │ │ ●─●─●─●  │
│                                                      │ │   브랜드  │
│                                                      │ │          │
│                                                      │ │ 흐름 요약 │
│                                                      │ │ ─────────│
│                                                      │ │ 최근 7일  │
│                                                      │ │ 동안 142개│
│                                                      │ │ 18% 연결됨│
│                                                      │ │          │
│                                                      │ │ 사고 리듬│
│                                                      │ │ ─────────│
│                                                      │ │ 오전 9-11│
│                                                      │ │ ▁▂█▅▃▁  │
│                                                      │ │          │
└──────────────────────────────────────────────────────┘ └──────────┘
```

### 구성 요소

| ID | 이름 | 비고 |
|---|---|---|
| SIG-keywords | 반복 키워드 + 빈도 | 상위 5-10개, 클릭 시 해당 메모만 보기 |
| SIG-cluster | 군집 그래프 | 힘 기반 레이아웃의 미니 그래프, 노드 = 메모 |
| SIG-summary | 자연어 흐름 요약 | 1-2 문장, Claude Haiku 출력 |
| SIG-rhythm | 사고 리듬 | 시간대별 메모 빈도를 보여주는 스파크 차트 |
| SIG-close | × 닫기 | ESC 또는 클릭 |

### 상태

- **데이터 부족 (메모 < 10)**: "조금 더 머무르면 패턴이 보일 거예요" 안내, 섹션은 빈 상태
- **AI 처리 거부**: 패널 자체가 잠김, "시그널스를 켜면 패턴이 보입니다" + 토글
- **쿼터 소진**: 마지막 캐시 + "이번 달 인사이트는 여기까지" + Pro 전환 안내 (잔잔한 톤)
- **로딩**: 각 섹션 스켈레톤

### 인터랙션

- 키워드 클릭 → 작업공간에서 해당 메모만 보기
- 군집 노드 클릭 → 해당 메모 카드로 이동
- 인사이트 [×] → `dismissedAt` 기록, 같은 인사이트는 24시간 동안 재노출 안 함

---

## 14. SCR-mobile-feed — "머무는 생각"

**목적**: 모바일 진입 시 기본 화면. 캡처 + 재노출.
**진입**: 모바일 앱 시작 / PWA 홈 화면 아이콘.
**플랫폼**: 모바일 (320~430px)
**케이스** (블루프린트 대응): `default` (보드 가로 스크롤 + 필터 탭 + 카드 격자) / `empty` (보드 가로 스크롤 숨김, "첫 생각을 남겨보세요" 안내) / `offline` (상단 1px 띠 "오프라인 — AI는 잠시 멈춰요", 데이터 입력은 정상)

### 레이아웃

```
┌────────────────────────────┐
│ 9:41              ●●●● 📶   │
├────────────────────────────┤
│ 머무는 생각        ⋯  [🔍] │
├────────────────────────────┤
│                            │
│  ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ │
│  │  │ │  │ │  │ │  │ │  │ │  ← board thumbnails
│  │  │ │  │ │  │ │  │ │  │ │     (가로 스크롤)
│  └──┘ └──┘ └──┘ └──┘ └──┘ │
│  브랜드 사용자 UX  커피  콘텐츠│
│                            │
│ [전체][보드][메모][이미지]…│  ← 필터 탭 (가로 스크롤)
│ ──── ─────────────────     │
│                            │
│  ┌─────────────┐ ┌────────┐│
│  │"신뢰는 정보가│ │First   ││
│  │아니라, 안심의│ │Purchase││
│  │순간에 생긴다"│ │경험    ││
│  │2시간 전·#안심│ │사람들은││
│  └─────────────┘ │첫 구매…││
│                  │5시간 전 ││
│  ┌─────────────┐ └────────┘│
│  │ 메모        │            │
│  │ 너무 많은   │ ┌────────┐│
│  │ 정보는 오히려│ │ 타임라인││
│  │ 갈증을 만든다│ │ 영상… ▷││
│  │ - 일 보여주기│ │ vimeo  ││
│  │ - 백탁 보여… │ └────────┘│
│  │ 5시간 전     │            │
│  └─────────────┘            │
│                            │
├────────────────────────────┤
│   [+]            [Aa]      │  ← 하단 캡처 바
└────────────────────────────┘
```

### 구성 요소

| ID | 이름 | 비고 |
|---|---|---|
| M-header | 상단 헤더 | "머무는 생각" + 검색 + 메뉴 |
| M-board-strip | 보드 썸네일 가로 스크롤 | 최근 사용 5-7개 |
| M-filter | 필터 탭 | `전체 / 보드 / 메모 / 이미지 / 링크 / 파일` |
| M-card-grid | 카드 격자 (1-2열 반응형) | 메모 색상 = 분위기 컬러 |
| M-fab | 하단 캡처 바 | [+] 빠른 추가 / [Aa] 텍스트 즉시 입력 |

### 상태

- **비어있음**: 보드 가로 스크롤 숨김, 카드 영역에 "첫 생각을 남겨보세요" 안내
- **오프라인**: 상단에 1px 띠 "오프라인 — AI는 잠시 멈춰요" 잔잔하게
- **AI 처리 거부**: 필터에 "AI 제안" 탭 없음

### 인터랙션

- 카드 탭 → 전체 보기 (모달, 편집 가능)
- 카드 길게 누름 → 보드 이동 / 색상 / 삭제 메뉴
- 당겨서 새로고침: 인사이트 갱신
- 하단 [+] 탭 → SCR-capture-bar 하단 시트 열기
- 하단 [Aa] 탭 → 그 자리에서 텍스트 입력

---

## 15. SCR-onboarding — 첫 진입 (안티-튜토리얼)

**목적**: 안내 없이 자연스러운 첫 캡처로 이끈다. 튜토리얼 모달 금지.
**진입**: 최초 방문 시 1회만.
**플랫폼**: 데스크톱 + 모바일
**케이스** (블루프린트 대응): `initial` (로고 + 한 줄 카피 + 빈 입력) / `after-input` (⏎ 후 첫 카드 등장 전환 → 빈 캔버스로)

### 레이아웃 (데스크톱)

```
┌────────────────────────────────────────────────────────────────┐
│                                                                │
│                                                                │
│                    moss.                                       │
│                                                                │
│                                                                │
│           생각은, 머무는 동안 형태가 된다.                     │
│                                                                │
│                                                                │
│           [ 지금은 그냥 남겨두셔도 좋아요...        ]          │
│                                                                │
│                                                       ⏎ 시작  │
│                                                                │
│                                                                │
│                                                                │
│           로그인 없이 시작 · 데이터는 이 기기에만 저장됩니다    │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### 구성 요소

| ID | 이름 | 비고 |
|---|---|---|
| ONB-logo | "moss." 로고 + 한 줄 카피 | 큰 글자, 호흡감 |
| ONB-input | 단일 텍스트 입력 | 안내 문구 = "지금은 그냥 남겨두셔도 좋아요..." |
| ONB-cta | ⏎ 시작 | 입력 즉시 첫 메모로 저장 후 SCR-workspace로 이동 |
| ONB-trust-note | 신뢰 카피 (작게) | "로그인 없이 시작 · 데이터는 이 기기에만 저장됩니다" |

### 상태

- **첫 진입**: 입력칸 비어있음
- **입력 후**: ⏎로 첫 메모 저장 → 짧은 전환(페이드 + 카드 등장) → 빈 캔버스로 이동, 방금 적은 메모가 캔버스 중앙에 놓여 있음
- **건너뛰기**: 입력 비운 채 [건너뛰기] 클릭 → 빈 캔버스로 이동
- **PWA 설치 안내**: 첫 메모 후 24시간 내 1회만 표시, 강요 없음

### 인터랙션

- 절대 하지 않을 것: 5단계 튜토리얼, 화살표 가이드, 강제 가입, 마우스를 따라다니는 툴팁
- ESC: 건너뛰기 (빈 캔버스로)
- ⏎: 저장 + 진입

---

## 16. SCR-export — Markdown / JSON Canvas / .moss

**목적**: 데이터 소유권 확인. 무손실 백업.
**진입**: 설정 메뉴 / 보드 우클릭 메뉴 / `Cmd+E`
**플랫폼**: 데스크톱 모달 (모바일은 공유 시트)
**케이스** (블루프린트 대응): `modal-default` (범위 + 포맷 선택) / `in-progress` (버튼이 진행 막대로 변환) / `success-toast` (완료 토스트 "moss-export-YYYY-MM-DD.zip 다운로드됨")

### 레이아웃 (데스크톱 모달)

```
┌──────────────────────────────────────────────────────────────┐
│  내보내기                                              ×    │
│  ────────────────────────────────────────────────────────── │
│                                                              │
│  범위                                                        │
│  ◉ 전체 (보드 7개 · 메모 142개)                              │
│  ○ 현재 보드 (Brand Exploration · 24개)                      │
│  ○ 선택한 메모 (3개)                                         │
│                                                              │
│  포맷                                                        │
│  ◉ .moss bundle  — 무손실 백업/이전용 (권장)                  │
│  ○ Markdown      — 메모 1개 = 파일 1개 (호환)                │
│  ○ JSON Canvas   — Obsidian 호환 (캔버스 좌표 보존)          │
│                                                              │
│  ────────────────────────────────────────────────────────── │
│                                          [취소]  [내보내기] │
└──────────────────────────────────────────────────────────────┘
```

### 구성 요소

| ID | 이름 | 비고 |
|---|---|---|
| EXP-scope | 범위 라디오 | 전체 / 현재 보드 / 선택 |
| EXP-format | 포맷 라디오 | .moss / Markdown / JSON Canvas |
| EXP-cta | 내보내기 버튼 | 클릭 시 다운로드 |

### 상태

- **진행 중**: 버튼이 진행 막대(progress bar)로 변환
- **완료**: 토스트 "moss-export-2026-05-19.zip 다운로드됨"
- **실패 (용량/네트워크)**: 본문 안에 에러 표시 + 재시도

---

## 17. SCR-paywall — Pro 안내

**목적**: Free 쿼터 소진 또는 Pro 기능 시도 시 부드러운 안내.
**진입**: AI 쿼터 소진 / 여러 기기 동기화 시도 / 설정 → 업그레이드
**플랫폼**: 데스크톱 모달 (모바일은 전체 화면 시트)
**케이스** (블루프린트 대응): `quota-exhausted` (월 AI 100회 소진) / `pro-feature-blocked` (동기화·자동 백업 등 Pro 전용 기능 시도) / `soft-warning-toast` (90% 도달 시 모달 아닌 잔잔한 토스트)

### 레이아웃

```
┌──────────────────────────────────────────────────────────────┐
│                                                        ×    │
│                                                              │
│   이번 달 AI 인사이트가 소진되었어요.                        │
│                                                              │
│   메모와 캔버스는 그대로 작동합니다.                          │
│   AI를 더 사용하려면 Pro로 전환해보세요.                      │
│                                                              │
│   ────────────────────────────────────────────────────────  │
│                                                              │
│           Free                  Pro                          │
│                                                              │
│      • 캔버스 + 로컬 저장   • 모든 Free 기능                  │
│      • 월 100회 AI         • AI 무제한 (fair use)            │
│      • 단일 디바이스        • 멀티 디바이스 sync              │
│                            • 자동 백업                       │
│                                                              │
│                            월 $8 · 연 $80 (16% 절약)          │
│                                                              │
│      [지금은 괜찮아요]              [Pro로 전환]              │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 구성 요소

| ID | 이름 | 비고 |
|---|---|---|
| PW-headline | 상황 맞춤 헤드라인 | "AI 인사이트 소진" / "동기화는 Pro 기능" 등 |
| PW-reassure | 안심 카피 | "메모와 캔버스는 그대로 작동합니다" — 핵심 |
| PW-compare | Free vs Pro 비교 | 2열, 차이만 강조 |
| PW-price | 가격 + 절약율 | 월/연 토글 |
| PW-cta | [Pro로 전환] | Stripe 결제 페이지로 이동 |
| PW-dismiss | [지금은 괜찮아요] | 모달 닫기, 24시간 동안 재노출 안 함 |

### 상태

- **Free 쿼터 부드러운 경고 (90% 도달)**: 모달 아닌 토스트로 "이번 달 AI 인사이트가 곧 소진돼요"
- **Pro 결제 진행 중**: Stripe 결제 페이지로 외부 이동
- **결제 후 복귀**: 토스트 "Pro로 전환되었어요" + 쿼터 해제

### 인터랙션

- ESC / [지금은 괜찮아요] → 닫기, 24시간 동안 재노출 안 함
- 월/연 토글: 가격 + 절약 계산 즉시 갱신

---

## 18. 화면 간 이동(네비게이션)

**메인 화면 = SCR-workspace.** 홈·보드 목록 같은 별도 화면은 존재하지 않는다. 모든 메모·큐레이팅·보드 전환이 이 한 화면에서 일어난다.

```
[SCR-onboarding]  (최초 1회)
       │
       ▼
[SCR-workspace] ←──────── 앱 시작 기본 (시스템 보드 "머무는 생각")
   ┌──────────────────────────────────────────────────┐
   │ 좌측 사이드바 (접이식 220px / 64px)              │
   │                                                  │
   │ ┌─ 도구 (끌기 시작점) ─────────────────────────┐ │
   │ │ Aa / ✎ / ⌒ / ⌘ / ☑ / 🖼 / 🔗 / 🎤 / 📎 / </>│ │
   │ │  → 캔버스에 놓기 → 카드 생성                  │ │
   │ └─────────────────────────────────────────────┘ │
   │ ─────────                                        │
   │ ┌─ 메뉴 (클릭) ────────────────────────────────┐ │
   │ │ ✦  시그널스 → SCR-signals (옆에서 미끄러짐)  │ │
   │ │ ↗  내보내기 → SCR-export (모달)              │ │
   │ └─────────────────────────────────────────────┘ │
   │                                                  │
   │ 상단 헤더의 보드 드롭다운 ("머무는 생각 ▾")     │
   │  ├ 머무는 생각 (시스템, 고정)                   │
   │  ├ Brand Exploration                            │
   │  ├ unfinished thoughts                          │
   │  ├ ...                                          │
   │  └ + 새 보드 (시작 템플릿 5종)                  │
   └──────────────────────────────────────────────────┘

모바일 (SCR-mobile-feed): 사이드바 없음. 상단에 보드 드롭다운, 하단에 캡처 바.

모달·시트 (어디서나):
- SCR-capture-bar  (Cmd+Shift+N 단축키 보조 경로 / 모바일 메인)
- SCR-export       (Cmd+E)
- SCR-paywall      (쿼터 소진 / Pro 기능 시도 시 자동)
```

---

## 19. 공통 UI 원칙

### 빈 상태 / 로딩 / 에러

| 상태 | 톤 |
|---|---|
| 빈 상태 | 안티-튜토리얼. "아직 아무것도 없네요!" 류의 카피 **금지**. "조금 더 머무르면 보일 거예요" 톤. |
| 로딩 | 스켈레톤 + 잔잔한 페이드. 스피너 회전은 느리게. |
| 에러 | "다시 시도" 우선. 본문 손실 방지. AI 실패가 메모 기능엔 영향 없음을 명시. |
| 오프라인 | 상단 1px 띠 + "오프라인 — AI는 잠시 멈춰요". 데이터 입력은 정상 동작. |

### 모션 가이드

- 카드 등장: 80~120ms 이즈아웃 + 미세 회전(±1.5°)
- 모달 등장: 200ms 이즈아웃, 크기 0.96 → 1
- 연결선 그려지는 애니메이션: 400ms
- 토스트: 우하단, 3초 후 자동 사라짐
- 잔향(decay) 모션: 사라질 때 200ms로 흐려지며 살짝 위로 부유

### 접근성

- 모든 인터랙티브 요소 키보드 도달 가능
- 색상에만 의존하지 않음 (mood 컬러 + 텍스트 라벨)
- AI 옵트인 토글은 반드시 시각 + 텍스트 + 아이콘 3중 명시
- 한국어 폰트 크기 본문 최소 15px (한글 가독성)

---

# Part III — 기술

> Part I의 4개 상위 제약(웹 PWA / Local-first / API AI / Freemium)이 모든 기술 결정의 상위 제약이다. 충돌 시 이걸 우선한다.

## 20. 시스템 아키텍처

### 20-1. 전체 구성도

```
┌─────────────────────────────────────────────────────────┐
│  Client (Browser / PWA)                                  │
│                                                          │
│  ┌─────────────┐  ┌────────────┐  ┌──────────────────┐  │
│  │ UI Layer    │  │ State      │  │ Storage Layer    │  │
│  │ (React)     │←→│ (Zustand   │←→│ Dexie (IDB) +    │  │
│  │ Canvas      │  │  + Yjs doc │  │ OPFS (blobs) +   │  │
│  │ renderer    │  │  optional) │  │ in-mem vectors   │  │
│  └─────────────┘  └────────────┘  └──────────────────┘  │
│                          │                               │
│                          ▼                               │
│                  ┌──────────────┐                        │
│                  │ AI Client    │  (only when online +   │
│                  │ (rate-limit) │   user opted-in)       │
│                  └──────┬───────┘                        │
└─────────────────────────┼────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────┐
│  Backend Proxy (Stateless, edge-deployed)                │
│  - AI key holder (Claude / OpenAI)                       │
│  - Quota / rate limit per user                           │
│  - Auth (Supabase Auth or Clerk)                         │
│  - Stripe webhook                                        │
│  - Optional: y-websocket sync for Pro                    │
└─────────────────────────────────────────────────────────┘
              │                            │
              ▼                            ▼
       ┌───────────┐                ┌──────────────┐
       │ Claude /  │                │ Stripe       │
       │ OpenAI    │                │              │
       └───────────┘                └──────────────┘
```

### 20-2. 계층 책임

| 계층 | 역할 | 무엇을 가지지 않는가 |
|---|---|---|
| 클라이언트 | 모든 메모·보드·연결 데이터, 렌더링, 오프라인 동작 | API 키, 결제 정보, 다른 사용자 데이터 |
| 백엔드 프록시 | AI 호출 중계, 인증, 결제, (Pro만) 동기화 릴레이 | 사용자 메모 본문 영구 저장 (Pro 동기화는 예외 — 암호화된 상태로 보관) |
| 외부 API | LLM 추론, 결제 처리 | — |

### 20-3. 데이터 흐름 원칙

1. **메모 본문은 기본적으로 서버를 통과하지 않는다.** AI 호출 시점에만, 옵트인된 메모만 일시적으로 통과.
2. **백엔드 proxy는 stateless를 지향.** 영구 상태는 인증/결제/(Pro) sync 릴레이뿐.
3. **클라이언트가 오프라인이어도 AI 외 모든 기능이 작동해야 한다.**

---

## 21. 데이터 모델

### 21-1. 핵심 엔티티

```ts
// 모든 ID는 ULID. 시간 정렬 가능하고 충돌 없는 분산 식별자.

type Note = {
  id: ULID;
  type: 'text' | 'handwriting' | 'mindmap' | 'highlight' | 'checklist'
      | 'image' | 'link' | 'audio' | 'file' | 'code';
  content: NoteContent;       // type별 union
  position?: { x: number; y: number; w?: number; h?: number };
  boardId?: ULID;             // null이면 무소속(시스템 보드에서만 노출)
  color?: AccentColor;        // 분위기 컬러
  rotation?: number;          // -1.5° ~ +1.5° 종이 질감
  aiOptOut?: boolean;         // true면 AI에 절대 보내지 않음
  createdAt: ISO8601;
  updatedAt: ISO8601;
  lastVisitedAt?: ISO8601;    // revisit time 측정
  visitCount: number;
};

type NoteContent =
  | { kind: 'text'; markdown: string }
  | { kind: 'handwriting'; strokes: Stroke[]; preview: BlobRef }
  | { kind: 'mindmap'; root: MindNode }
  | { kind: 'highlight'; quote: string; source?: string }
  | { kind: 'checklist'; items: { text: string; done: boolean }[] }
  | { kind: 'image'; blob: BlobRef; ocrText?: string; alt?: string }
  | { kind: 'link'; url: string; title?: string; summary?: string; thumb?: BlobRef }
  | { kind: 'audio'; blob: BlobRef; transcript?: string; durationMs: number }
  | { kind: 'file'; blob: BlobRef; filename: string; mime: string }
  | { kind: 'code'; lang: string; code: string };

type BlobRef = { opfsPath: string; size: number; sha256: string };

type Board = {
  id: ULID;
  name: string;               // 빈 문자열 허용 ("이름 없음")
  template?: 'free' | 'mind' | 'project' | 'research' | 'diary';
  cover?: BlobRef;
  mood?: AccentColor;
  viewport?: { zoom: number; pan: { x: number; y: number } };
  createdAt: ISO8601;
  updatedAt: ISO8601;
};

type Connection = {
  id: ULID;
  fromNoteId: ULID;
  toNoteId: ULID;
  source: 'manual' | 'ai-suggested';
  status: 'active' | 'rejected';   // AI 제안 거절 시 음의 신호
  label?: string;                  // 손글씨 라벨
  createdAt: ISO8601;
};

type Embedding = {
  noteId: ULID;
  vector: Float32Array;            // 1024d (text-embedding-3-small)
  model: string;                   // 'openai/text-embedding-3-small@2024'
  contentHash: string;             // 변경 감지
  createdAt: ISO8601;
};

type AiInsight = {
  id: ULID;
  kind: 'keyword' | 'cluster' | 'connection' | 'flow' | 'rhythm';
  payload: unknown;                // kind별 union
  dismissedAt?: ISO8601;
  acceptedAt?: ISO8601;            // 채택 시
  createdAt: ISO8601;
};

type User = {
  id: ULID;                        // 로컬 전용 ID
  plan: 'free' | 'pro';
  aiQuota: { used: number; resetAt: ISO8601 };
  cloudUserId?: string;            // Pro면 백엔드 계정 ID
  prefs: UserPrefs;
};
```

### 21-2. 인덱스 (Dexie)

| Store | Primary Key | Indexes |
|---|---|---|
| `notes` | `id` | `boardId`, `type`, `createdAt`, `updatedAt`, `lastVisitedAt` |
| `boards` | `id` | `updatedAt` |
| `connections` | `id` | `fromNoteId`, `toNoteId`, `[fromNoteId+toNoteId]` |
| `embeddings` | `noteId` | `model` |
| `aiInsights` | `id` | `kind`, `dismissedAt`, `createdAt` |
| `user` | `id` | (single row) |

### 21-3. 마이그레이션 정책

- Dexie 버전을 단조 증가시키며 `.upgrade()` 콜백으로 in-place migration.
- 클라이언트는 자기 schema 버전을 알고, 더 낮은 버전 데이터를 보면 자동 업그레이드.
- 다운그레이드는 지원하지 않음 (사용자 export 권장).

---

## 22. 저장소 전략

### 22-1. 결정: **Dexie (IndexedDB) + OPFS**

**선택 이유:**
- IndexedDB는 모든 브라우저 표준이고, Dexie는 그 위의 가장 검증된 wrapper.
- SQLite WASM은 강력하지만 초기 번들 크기와 트랜잭션 복잡도가 부담. 1000+ 메모 사용자가 흔해지면 검토.
- OPFS(Origin Private File System)는 큰 blob(이미지/음성/파일) 저장에 적합 — IndexedDB BLOB은 큰 파일에서 성능 저하.

**구조:**
- 메타데이터 / 본문(텍스트, JSON) → IndexedDB via Dexie
- Blob (이미지, 음성, 파일, 손글씨 preview) → OPFS, IDB에는 `opfsPath`만 저장
- 벡터 → IndexedDB (1024d × 4byte × 메모 수. 10,000 메모 ≈ 40MB, 충분)

### 22-2. 용량 / 정리

- 브라우저 quota는 보통 사용 가능 디스크의 ~60%. 일반 사용자에게 충분.
- `navigator.storage.persist()` 호출로 영속성 요청. 사용자에게 권한 안내.
- 30일 이상 미접근 + AI 사용 안 한 blob은 오프로드(서버 백업)할지 추후 결정.

---

## 23. 동기화 전략 (Pro 기능, MVP 이후)

### 23-1. 결정: **Yjs (CRDT) + y-websocket relay**

**선택 이유:**
- CRDT는 local-first의 정답에 가깝다. 마지막-쓰기-승리(LWW)는 멀티 디바이스에서 데이터 손실 위험.
- Yjs는 가장 성숙한 JS CRDT 라이브러리.
- y-websocket 자체 호스팅이 부담스럽다면 y-sweet (Drifting in Space) 같은 매니지드 옵션도 호환.

### 23-2. 단계적 도입

| 단계 | 범위 | 비고 |
|---|---|---|
| MVP | 동기화 없음. 단일 기기 로컬 우선 | 내보내기·가져오기로 수동 이동 |
| 2단계 (Pro β) | Yjs로 메모·보드 동기화 | 종단간(end-to-end) 암호화 (사용자 패스프레이즈) |
| 3단계 | 충돌 시각화, 기기 목록 관리 | — |

### 23-3. 암호화

- 동기화 채널의 데이터는 **클라이언트에서 암호화 후 전송**. 서버는 암호문만 본다.
- 키 도출: PBKDF2(사용자 비밀번호 + 기기 솔트). 비밀번호 분실 시 복구 불가 (사용자에게 명시).

---

## 24. AI 파이프라인

### 24-1. 캡처 → 처리 흐름

```
[메모 저장]
    │
    ▼
[contentHash 계산] ── 변경 없음 → 종료
    │
    ▼ aiOptOut 체크
[AI 보내기 큐 등록] ── opt-out → 종료
    │
    ▼ 온라인 + quota 잔여
[배치(N개)로 backend proxy 호출]
    │
    ▼
[backend → embedding API (OpenAI text-embedding-3-small)]
    │
    ▼
[embedding을 client로 응답]
    │
    ▼
[IDB embeddings store에 저장]
    │
    ▼
[로컬에서 KNN 재계산 → 연결 후보 갱신]
    │
    ▼ (별도 트리거: 일정 주기 / 사용자 요청)
[키워드 / 클러스터 / 흐름 요약 = LLM 호출]
    │
    ▼
[AiInsight로 저장 → 시스템 보드/Signals에 노출]
```

### 24-2. 배치 전략

- 메모 변경 후 5초 debounce → 모아서 백엔드 호출
- 한 번에 최대 32개 (OpenAI batch embedding 효율 구간)

### 24-3. 모델 매트릭스

| 작업 | 모델 (1st) | 모델 (대체) | 비고 |
|---|---|---|---|
| 임베딩 | OpenAI `text-embedding-3-small` 1024d | Voyage AI | 가격/품질 절충 |
| 자연어 요약 (흐름 / 군집 설명) | Claude Haiku 4.5 | GPT-5 mini | 1-2문장 톤이 중요. Anthropic 톤 선호 |
| 연결 라벨 제안 | Claude Haiku 4.5 | — | 1-3단어 |
| 이미지 OCR | Tesseract.js (로컬) | Claude Sonnet vision (Pro) | 로컬 우선, 정확도 부족 시 Pro에서 vision |
| 음성 받아쓰기 | Whisper API | Web Speech API (로컬 보조) | 한국어 정확도 우선 |

---

## 25. 연결 알고리즘

### 25-1. 후보 점수

```
score(a, b) = cos(emb_a, emb_b) × temporal_decay × user_signal
```

| 요소 | 정의 |
|---|---|
| `cos` | 임베딩 코사인 유사도 (0~1) |
| `temporal_decay` | `exp(-Δdays / 30)` × 0.4 + 0.6 (오래된 메모도 일정 가치 유지) |
| `user_signal` | 같은 보드 = 1.2, 사용자 거절 이력 있음 = 0.5 |

### 25-2. 임계값

- `score ≥ 0.78` → "오늘의 연결" 후보
- `0.65 ≤ score < 0.78` → 약한 연결 (시각적으로만 흐릿하게)
- `score < 0.65` → 비표시

임계값은 사용자별 dismiss/accept 비율로 자동 조정 (개인화).

### 25-3. 다양성

- 같은 클러스터 내 페어는 하루 1회만 제안 (중복 피로 방지).
- "멀리 있는 연결"을 우대: 같은 보드 내 페어보다 다른 보드 페어를 가산.

---

## 26. 캔버스 렌더링

### 26-1. 결정: **DOM + virtualization (MVP) → Canvas2D fallback (Phase 2)**

**이유:**
- DOM 기반은 텍스트 입력/접근성/디버깅이 압도적으로 쉽다.
- 100~200개 메모까지는 `react-window` 같은 viewport culling으로 충분.
- 그 이상에서는 Canvas2D 렌더러로 전환 (Excalidraw 방식).

### 26-2. 줌 구현

- CSS `transform: scale()` + `translate()`로 viewport 변환.
- 메모 자체는 1:1로 렌더, transform이 변환을 흡수. 텍스트 선명도 유지.

### 26-3. 성능 가드

- 메모 수 200 초과 → Canvas2D 렌더러로 자동 전환 안내
- 사용자에게 "지형도 모드"(축소 시 thumbnail-only)도 제공

---

## 27. 인증과 결제

### 27-1. 인증

- **Supabase Auth** (이메일 매직 링크 + Google OAuth)
- Free 사용자도 선택적으로 가입 가능(백업·AI 쿼터 추적용) 또는 익명 사용 가능
- 익명 → 로그인 시 로컬 데이터 보존

### 27-2. 결제

- **Stripe** 우선 (글로벌). 카드 결제 + 구독 관리 + 영수증
- 국내 사용자 비율이 50%를 넘어가면 **토스페이먼츠** 병행
- Stripe → 백엔드 웹훅 → `user.plan` 갱신

### 27-3. 가격 (미정, 초안)

| 플랜 | 월 | 연 | 포함 |
|---|---|---|---|
| Free | $0 | — | 캔버스 + 로컬 저장 + 월 100회 AI 호출 |
| Pro | $8 | $80 | AI 무제한 (합리적 사용 한도 내) + 여러 기기 동기화 + 우선 지원 |

가격은 출시 후 2개월 데이터로 검증한다.

---

## 28. 프라이버시와 데이터 흐름

### 28-1. 사용자에게 보이는 약속

1. 메모 본문은 기본적으로 사용자 기기에만 있다.
2. AI 사용을 끄면 어떤 메모도 서버를 거치지 않는다.
3. AI 사용을 켜도, 특정 메모는 `aiOptOut`으로 개별 제외 가능.
4. Pro 동기화는 종단간(end-to-end) 암호화. 서버는 암호문만 본다.

### 28-2. AI 호출 시 UX

- AI 호출 직전에 "어떤 메모가 분석되는지" UI에 표시 (개수 + 일부 미리보기)
- `aiOptOut` 메모는 시각적으로 다른 표식 (예: 자물쇠 아이콘)
- 사용자가 AI 응답을 받기 전에 취소 가능

### 28-3. 로깅 정책

- 백엔드 프록시 로그: 사용자 ID + 호출 시각 + 토큰 수만. **메모 내용은 로그에 남기지 않는다.**
- 외부 API 호출 응답도 저장하지 않음 (단순 중계).

---

## 29. 내보내기 / 가져오기

### 29-1. 포맷

- **Markdown**: 메모 1개 = 파일 1개. 폴더로 보드 표현. 손실이 가장 크지만 표준.
- **JSON Canvas** (Obsidian): 캔버스 좌표/연결 보존. 보드 단위로.
- **`.moss` 번들**: 자체 포맷 (zip 안에 JSON + 첨부 파일). 무손실. 백업·이전용.

### 29-2. 가져오기

- `.moss` 번들 단방향 가져오기
- 추후: Notion, Obsidian, Pinterest 가져오기 (3단계)

---

## 30. i18n

### 30-1. 결정: **한국어 단독 출시 (v1)**

- 정성적 톤("지금은 그냥 남겨두셔도 좋아요" 등)이 제품의 본질. 영어 번역으로 톤 손실 위험.
- 영어는 출시 후 6개월 시점 검토.
- 코드에는 `i18n` 구조를 미리 둔다 (한국어 messages만 채움).

### 30-2. 라이브러리

- `react-intl` 또는 `next-intl` (Next.js 사용 시)
- 메시지 파일: `messages/ko.json`

---

## 31. 관측 (Observability)

### 31-1. 클라이언트

- **PostHog** (셀프 호스팅 옵션 있음, 프라이버시 친화)
- 이벤트: 캡처, 보드 열기, 연결 수락·거절, AI 쿼터 도달, 내보내기
- **본문은 절대 이벤트에 포함하지 않는다.** (타입·개수·지속 시간만)

### 31-2. 백엔드 프록시

- 구조화 로그 (JSON) → Cloudflare Workers + Logpush 또는 Vercel Analytics
- 지표: AI 호출 P50/P95 응답 시간, 에러율, 쿼터 포화도

### 31-3. 에러

- Sentry (클라이언트 + 서버). 사용자 메모는 자동 마스킹.

---

## 32. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | **Next.js 15 (App Router)** | PWA, SSR, 백엔드 프록시를 동일 레포에 |
| 언어 | **TypeScript (strict)** | — |
| UI | **React 19 + Tailwind v4** | 빠른 프로토타이핑 |
| 상태 관리 | **Zustand** + **Yjs** (Pro 동기화) | Redux는 과함 |
| 저장소 | **Dexie** (IndexedDB) + **OPFS** | §22-1 |
| 캔버스 | DOM + `react-window` (MVP) | §26-1 |
| 백엔드 | **Next.js Route Handlers** on **Vercel Edge** | 지연 최소화 |
| 인증 | **Supabase Auth** | 매직 링크 + OAuth |
| 결제 | **Stripe** | — |
| AI | **Anthropic SDK** + **OpenAI SDK** | 이중 공급자 |
| 분석 | **PostHog** | 셀프 호스팅 가능 |
| 에러 추적 | **Sentry** | — |
| 테스트 | **Vitest** + **Playwright** | — |

---

## 33. 빌드와 배포

- 모노레포 불필요 (단일 Next.js 앱).
- CI: GitHub Actions → Vercel.
- 환경: `dev` / `staging` / `prod`.
- 비밀 관리: Vercel Env + Doppler (옵션).

---

# Part IV — 프로젝트

## 34. MVP 범위

### 34-1. 포함

- 웹 PWA (반응형 — 데스크톱 + 모바일 동일 코드)
- 로컬 우선(Local-first) 저장 (IndexedDB / OPFS)
- **단일 메인 화면** SCR-workspace (도구 사이드바 + 캔버스)
- 좌측 접이식 도구 사이드바 (캡처 10종 끌기 시작점 + 시그널스·내보내기 메뉴)
- 캡처 10종 끌어다 놓기 (단축키 / 모바일 캡처 바는 보조 경로)
- **시스템 보드 "머무는 생각"** (첫 진입, AI 자동 큐레이팅, 삭제 불가)
- 자유 보드 (거대 캔버스, 줌, 연결선)
- 보드 선택 드롭다운 (홈·보드 목록 화면 대체)
- AI 군집 + 키워드 + 연결 제안 (API 프록시 경유)
- 재노출(resurfacing) (시스템 보드 카드로 표현)
- 느슨한 연결선 (수동 + AI 추천)
- 모바일 "머무는 생각" 피드 (SCR-mobile-feed, 시스템 보드의 모바일 표현)
- 5종 시작 템플릿
- 오늘의 질문 / 생각 일기 / 연결 히스토리 (시스템 보드 내 위젯 카드)
- 프리미엄 결제 게이트 (Free vs Pro 진입 흐름)

### 34-2. 제외

- 여러 기기 동기화 (Pro 기능으로 분리, MVP 이후)
- 팀 협업 / 실시간 공동 편집
- 네이티브 데스크톱 / 모바일 앱
- 업무·태스크 관리 / 캘린더 / 프로젝트 관리
- AI 글 대필 생성
- 정량 생산성 지표

---

## 35. 단계별 로드맵

| 단계 | 기간 | 범위 |
|---|---|---|
| 0. 기반 다지기 (Foundation) | 2주 | Next.js 세팅, Dexie 스키마, 기본 캡처 (텍스트 + 이미지) |
| 1. 캡처 + 단일 보드 | 4주 | 10종 캡처, 자유 보드 1개, 줌, 수동 연결 |
| 2. 시스템 보드 + 재노출 | 3주 | 머무는 생각 카드, 다시 떠오른 생각 |
| 3. AI 시그널스 | 4주 | 임베딩 파이프라인, 키워드, AI 연결 제안 |
| 4. 다중 보드 + 템플릿 | 2주 | 여러 보드, 5종 템플릿, 보드 선택 드롭다운 |
| 5. 프리미엄 + 결제 | 2주 | Stripe, 쿼터, 가입 |
| **MVP 출시** | — | (위 0~5 완료 시점, 약 17주) |
| 6. Pro 동기화 | 4주 | Yjs + 종단간 암호화 |
| 7. 내보내기·가져오기 | 2주 | Markdown / JSON Canvas / .moss |
| 8. 다듬기 (Polish) | 지속 | 손글씨, 음성 받아쓰기, OCR 정확도 |

---

## 36. 성공 지표

| 지표 | 측정 방법 |
|---|---|
| 생각 회귀율 (Thought Return Rate) | (7일 내 재방문한 메모 수) / (저장된 메모 수) |
| 연결 생성률 (Connection Rate) | 메모당 평균 연결 수 (수동 + 수락된 AI 추천) |
| 재방문 체류 시간 (Revisit Time) | 같은 메모에 머문 누적 시간 |
| 초안 전환율 (Draft Conversion) | 메모를 씨앗 삼은 초안·보드 전환 비율 |
| 장기 유지율 (Long-term Retention) | D30 / D90 활성 사용자 비율 |
| Free → Pro 전환율 | 무료 사용자 중 Pro 결제 비율 (프리미엄 핵심 지표) |
| AI 제안 채택률 | AI 연결·응집 제안 중 사용자가 수락한 비율 |

기존 SaaS의 DAU / WAU / 세션 길이는 **참고용**으로만 본다. moss는 "자주 쓰는 툴"이 아니라 "계속 돌아오고 싶은 공간"이 목표다.

---

## 37. 리스크와 대응

| 리스크 | 영향 | 완화 |
|---|---|---|
| 브라우저 저장소 용량 초과 | 사용자 데이터 손실 위험 | `persist()` 요청 + 백업 알림 + 압축 + OPFS 활용 |
| AI 비용 폭증 (Free 남용) | 마진 잠식 | 쿼터 엄격 + 호출 빈도 제한 + 남용 탐지 |
| 캔버스 성능 (메모 200개 초과) | UX 저하 | 자동 모드 전환 + 측정·알림 |
| 외부 API 장애 (Claude·OpenAI) | AI 기능 중단 | 이중 공급자 + 단계적 기능 축소 (메모 기능은 계속 동작) |
| 로컬 우선 × 여러 기기 기대 충돌 | 이탈 | Pro에서 명확히 해결, Free 사용자에게는 내보내기로 안내 |
| 브라우저 지원 (Safari OPFS 등) | 일부 사용자 제한 | OPFS 미지원 시 IndexedDB blob으로 대체 (성능 저하 감수) |

---

## 38. 결정과 결정 로그

### 38-1. 확정 (2026-05-19, 상위 4개 제약)

- ✅ **플랫폼**: 웹 단독 (PWA)
- ✅ **데이터 소유권**: 로컬 우선 (브라우저 저장소)
- ✅ **AI 모델**: API 우선 (Claude / OpenAI), 백엔드 프록시 경유
- ✅ **수익 모델**: 프리미엄(Freemium) 구독

### 38-2. 기술 결정 로그

| # | 결정 | 대안 | 선택 이유 | 날짜 |
|---|---|---|---|---|
| 1 | Dexie + OPFS | SQLite WASM, RxDB | 검증된 표준 + 작은 번들. SQLite는 메모 1,000개 시점에 재검토 | 2026-05-19 |
| 2 | 동기화는 Yjs로 | Automerge, 자체 OT | JS 생태계 성숙도, y-websocket·y-sweet 호환 | 2026-05-19 |
| 3 | text-embedding-3-small 1024d | 3-large, Voyage | 비용·성능 절충, 한국어 충분 | 2026-05-19 |
| 4 | 요약은 Claude Haiku 4.5 | GPT-5 mini, Sonnet | 톤 + 비용 + 응답 속도 | 2026-05-19 |
| 5 | DOM 렌더러 (MVP) | Canvas2D, WebGL | 접근성 + 디버깅 + 메모 200개까지 충분 | 2026-05-19 |
| 6 | Supabase Auth | Clerk, Auth.js | 가격 + 셀프 호스팅 옵션 | 2026-05-19 |
| 7 | Stripe 우선 | 토스, Paddle | 글로벌 우선, 국내는 2단계에서 | 2026-05-19 |
| 8 | 한국어 단독 v1 | 한·영 동시 | 톤 손실 위험 | 2026-05-19 |
| 9 | Markdown + JSON Canvas + .moss | 자체 포맷만 | 표준 호환성 + 무손실, 둘 다 만족 | 2026-05-19 |
| 10 | Next.js + Vercel Edge | SvelteKit, 자체 인프라 | 백엔드 프록시 동일 레포 + 빠른 출시 | 2026-05-19 |
| 11 | 시스템 보드 "머무는 생각"으로 홈·보드 목록 흡수 | 홈·보드 별도 화면 유지 | 메인 화면 단일화로 정보 구조 단순. 재노출 가치는 보드 안 카드로 유지 | 2026-05-19 |
| 12 | 좌측 도구 사이드바 + 끌어다 놓기 | 우하단 [+] 캡처 모달 | 캡처가 화면의 본 흐름이 됨, 캔버스와 도구가 한눈에 | 2026-05-19 |

---

## 39. 아직 열린 질문

### 39-1. 기술 (구현 단계에서 결정)

1. **AI 쿼터 단위** — 호출 횟수 vs 토큰 수 vs 임베딩·요약 분리?
2. **익명 모드** — Free에서 가입 없이 어디까지 허용? (브라우저 닫으면 데이터 손실 위험)
3. **첫 백엔드 호스팅** — Vercel Edge로 시작 vs Cloudflare Workers
4. **분석 인프라** — PostHog vs Plausible vs 자체 구축
5. **온보딩 시나리오** — 빈 캔버스 첫 진입 시 어떻게 안내할지 (강한 안티-튜토리얼 기조와 충돌 가능)

### 39-2. 비즈니스 (출시 후 데이터로 검증)

1. **가격 검증** — Free 100회 / Pro $8가 적정한지 출시 후 2개월 데이터로 확인
2. **AI 데이터 프라이버시 세부** — 익명화·요약 후 전송할지? 어떤 메모를 보낼지 사용자가 직접 선택하는 UX?

---

## 40. 브랜드 내러티브

> 우리는 더 많은 정보를 저장하기 위해 이 제품을 만들지 않았다.
> 이미 충분히 많다.
>
> 필요한 것은:
> - 생각이 조금 더 오래 살아있을 공간
> - 흩어진 감각이 응집될 시간
> - 계속 다음으로 밀려나지 않는 흐름
>
> moss는 생산성 툴이 아니라,
> **생각이 사라지지 않게 만드는 환경**이다.
